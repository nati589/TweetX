import { createTheme } from "@mui/material";

export const theme = createTheme({
    palette: {
        primary: {
            main: '#fe748d',
            
        },
        secondary: {
            main: '#d0d0d0',
        },
        action: {
            active: '#fe748d'
        },
        custom: {
            main: '#000'
        }
    }
})